# quick-web

React + TypeScript web client for **quick**.

## Development

```sh
pnpm install
cp .env.example .env
pnpm dev
```

Backend must be reachable at `VITE_API_URL` (defaults to `http://localhost:8080`). See the `quick-backend` repository for how to bring up the local backend.
