export function DmHeaderMenu({
  peerUserId: _peerUserId,
  peerHandle: _peerHandle,
}: {
  peerUserId: string;
  peerHandle?: string;
}) {
  // Placeholder; the real menu (block, etc.) is wired in a later commit.
  return null;
}
